# Components

Reusable UI and feature components will live here once implementation begins.

Planned structure:

- `ui`: generic controls such as buttons, steppers, drawers, and badges.
- `features`: game-specific components such as dashboards, dice entry, trades, and history.

See [Components](../design/Components.md) and [Coding Standards](../docs/CodingStandards.md).

